import Typist from "react-typist";

function Loading() {
  return (
    <div>
      <Typist>
        {/* <span>Loading</span> */}
        <p>Hi</p>
        {/* <Typist.Backspace count={5} delay={5000} />
        <span>HI</span> */}
      </Typist>
    </div>
  );
}

export default Loading;
